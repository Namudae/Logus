spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/logus?serverTimezone=UTC&characterEncoding=UTF-8
#    url: jdbc:mysql://192.168.0.5:3306/logus?serverTimezone=UTC&characterEncoding=UTF-8
    username: aenergy
    password: aenergy
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#  org.hibernate.type: trace #스프링 부트 2.x, hibernate5
#  org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6

server:
  port: 8082

servlet:
  multipart:
    location: C:\\logusImage\\
    max-request-size: 30MB
    max-file-size: 10MB

file:
  dir: C:/logusImage/  # 실제 저장할 경로
#  temporaryLocation: C:/logusImage/temp  # 임시 저장 경로
#
#resourceHandlerUrl: /images/ # 이미지 파일 접근 uri

#contentImgLocation: C:/logusImage/save  # 실제 저장할 경로
#temporaryLocation: C:/logusImage/temp  # 임시 저장 경로
#resourceHandlerUrl: /images/ # 이미지 파일 접근 uri